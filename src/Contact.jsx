import { Element } from 'react-scroll';
import './Contact.css';

export default function Contact() {

    return (
        <Element id="contact" className="Contact element">
            <header>CONTACT</header>
        </Element>
    );
}